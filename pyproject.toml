[project]
name = "arxiv-research-agent"
version = "1.0.0"
description = "An arXiv research assistant built with Streamlit, LangGraph, and FastAPI."
readme = "README.md"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
