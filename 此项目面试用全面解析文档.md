# arXiv 论文助手 Agent — 面试用全面解析文档

> 本文档对整个项目进行系统性深度分析，涵盖架构设计、核心流程、功能实现细节，帮助你快速梳理项目全貌，应对面试中的各种技术提问。

---

## 目录

1. [项目定位与产品概述](#1-项目定位与产品概述)
2. [系统整体架构](#2-系统整体架构)
3. [技术栈全景](#3-技术栈全景)
4. [后端分层架构详解](#4-后端分层架构详解)
5. [Agent 编排系统（核心）](#5-agent-编排系统核心)
6. [ReAct Agent 设计（架构升级关键）](#6-react-agent-设计架构升级关键)
7. [意图识别系统](#7-意图识别系统)
8. [Tool 层设计与 Tool Calling 方案](#8-tool-层设计与-tool-calling-方案)
9. [Skill 设计（Skill-as-Tool 模式）](#9-skill-设计skill-as-tool-模式)
10. [MCP Server 设计](#10-mcp-server-设计)
11. [论文检索与重排（RAG + 多因子排序）](#11-论文检索与重排rag--多因子排序)
12. [Embedding 与语义相似度](#12-embedding-与语义相似度)
13. [Memory 记忆系统设计](#13-memory-记忆系统设计)
14. [Prompt 工程](#14-prompt-工程)
15. [可观测性（Trace-first Observability）](#15-可观测性trace-first-observability)
16. [订阅任务与定时调度](#16-订阅任务与定时调度)
17. [前端架构分析](#17-前端架构分析)
18. [数据库设计](#18-数据库设计)
19. [安全设计原则](#19-安全设计原则)
20. [项目亮点总结（面试重点）](#20-项目亮点总结面试重点)
21. [面试可能追问及回答思路](#21-面试可能追问及回答思路)

---

## 1. 项目定位与产品概述

### 1.1 项目是什么

这是一个**以聊天界面为主要入口的 arXiv 论文助手 Agent 系统**，用户可以通过自然语言完成论文检索、收藏、解析、本地文献库管理和每日订阅推送。

核心价值：用户不需要手动打开 arXiv 搜索、筛选、下载、保存和总结论文，系统全自动化完成。

### 1.2 完整工作流

```
用户聊天输入 "给我找 2 篇关于 agent 的论文"
→ 意图识别 (Intent Classifier)
→ Query 纠错和标准化 (Query Normalization)
→ arXiv API 检索候选论文 (Candidate K = 20)
→ 多因子重排 (Rerank: 语义相似度 + 关键词 + 新鲜度 + 用户偏好)
→ LLM 生成中文论文卡片摘要
→ 前端展示 Top N 论文卡片
→ 用户操作：收藏 → PDF下载入库 / 解析 → 全文解析 + 中文精读报告
→ 每个环节写入 Trace（全流程可观测）
```

### 1.3 功能模块

| 模块 | 功能 |
|------|------|
| 聊天式论文检索 | 自然语言输入，返回论文卡片（标题/摘要/核心问题/方法/结果） |
| 论文收藏 | 一键下载 PDF + 保存元数据到本地库 |
| 论文解析 | PDF 全文解析 + LLM 中文精读报告（11 章节 Markdown） |
| 本地论文库 | 搜索/筛选/删除/查看论文（支持 soft delete） |
| 每日订阅 | 定时检索 + 重排 + 自动收藏 + 邮件/飞书推送 |
| 可观测性 | 每次操作生成 trace_id，全流程步骤可视化 |

---

## 2. 系统整体架构

### 2.1 架构总览

```
┌─────────────────────────────────────────────────┐
│                 Vue 3 前端 (Vite + TypeScript)   │
│  ChatView / LibraryView / TraceView /            │
│  SubscriptionView / ReportViewer / SettingsView  │
│  Pinia Stores → API Layer → Axios                │
└──────────────────┬──────────────────────────────┘
                   │ HTTP (localhost:8000)
┌──────────────────▼──────────────────────────────┐
│              FastAPI 后端 (Python)               │
│  api/ (路由层) → services/ (业务逻辑)            │
│       ↓                                         │
│  agent/ (Agent 编排核心)                         │
│  ├── orchestrator.py    (主编排器)               │
│  ├── react_agent.py     (ReAct Agent)            │
│  ├── intent_classifier.py (意图识别)             │
│  ├── query_normalizer.py  (查询纠错)             │
│  ├── tool_registry.py     (工具注册中心)         │
│  ├── tool_schemas.py      (OpenAI Function Schema)│
│  ├── skill_registry.py    (技能注册中心)          │
│  ├── bootstrap.py         (启动工具注册)          │
│  └── prompts.py           (Prompt 模板)          │
│       ↓                                         │
│  tools/ (原子能力层)                             │
│  ├── arxiv_tool.py      (arXiv API)             │
│  ├── rerank_tool.py     (多因子重排)             │
│  ├── embedding_tool.py  (语义向量 + TF-IDF)      │
│  ├── report_tool.py     (论文卡片摘要 + 精读报告) │
│  ├── pdf_tool.py        (PDF 下载 + 解析)        │
│  ├── library_tool.py    (本地库 CRUD)            │
│  ├── llm_client.py      (多厂商 LLM 客户端)      │
│  ├── subscription_tool.py (订阅 CRUD)            │
│  ├── notify_tool.py     (邮件 + 飞书推送)        │
│  └── trace_tool.py      (全链路追踪)             │
│       ↓                                         │
│  models/ (SQLAlchemy 数据模型)                   │
│  db/ (SQLite + aiomysql 异步引擎)                │
│  jobs/ (APScheduler 定时任务)                    │
│  storage/ (本地文件系统管理)                     │
│  mcp_server/ (MCP 协议服务器)                    │
└──────────────────┬──────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────┐
│              外部服务                             │
│  arXiv API / LLM API / SMTP / 飞书 Webhook       │
└─────────────────────────────────────────────────┘
```

### 2.2 核心设计理念：四层 Agent 架构

这是本项目最核心的架构决策。项目从**固定编排型 Agent** 演进为**受控 ReAct Agent（Controlled ReAct Agent）**：

```
Layer 1: ReactAgent          → 决策规划层：模型决定下一步 action
Layer 2: Tool Registry       → 工具注册层：统一管理所有可调用工具
Layer 3: Skill Registry      → 技能编排层：把稳定工作流包装为高级工具
Layer 4: Business Tool/Service → 业务执行层：具体的工具实现
```

**关键设计原则**：Agent 不是完全自由的。每一步都经过：
1. 工具白名单校验（Tool Registry 检查）
2. 权限等级校验（read_only / write_safe / write_dangerous / expensive / external_send）
3. 业务规则校验（搜索阶段禁止全文解析、收藏和解析分开等）
4. Trace 记录（每一步的 reasoning_summary + action + observation）

---

## 3. 技术栈全景

### 3.1 前端技术栈

| 技术 | 用途 |
|------|------|
| Vue 3 | UI 框架 |
| TypeScript | 类型安全 |
| Vite | 构建工具 |
| Composition API (`<script setup lang="ts">`) | 组件编写范式 |
| Pinia | 状态管理（每个业务域一个 store） |
| Vue Router | 路由（懒加载） |
| Element Plus | UI 组件库 |
| Axios | HTTP 请求（拦截器做 key 格式转换） |

### 3.2 后端技术栈

| 技术 | 用途 |
|------|------|
| Python 3.12+ | 主语言 |
| FastAPI | Web 框架（CORS、路由、中间件） |
| Pydantic v2 | Schema 校验（入参/出参） |
| SQLAlchemy 2.0 | ORM（异步引擎） |
| SQLite + aiosqlite | MVP 数据库 |
| APScheduler | 定时任务调度（AsyncIOScheduler） |
| httpx | 异步 HTTP 客户端（arXiv API + LLM API） |
| PyMuPDF (fitz) | PDF 文本提取和段落解析 |

### 3.3 AI/Agent 层技术

| 技术 | 用途 |
|------|------|
| OpenAI Compatible API | LLM 调用（支持 OpenAI/DeepSeek/通义千问） |
| Function Calling (Tools API) | Tool Calling |
| Text Embedding API | 语义向量生成（text-embedding-3-small） |
| TF-IDF (自实现) | Embedding 不可用时的语义兜底 |

---

## 4. 后端分层架构详解

### 4.1 目录职责与调用链

严格按照分层架构设计，每层有明确的职责边界：

```
api/         → 路由层：只负责 HTTP 请求/响应处理
services/    → 业务逻辑层：编排业务流程
agent/       → Agent 编排层：意图识别、ReAct 决策
tools/       → 工具层：具体的原子能力（arXiv/PDF/LLM/邮件等）
models/      → 数据模型层：SQLAlchemy ORM 模型
schemas/     → Schema 层：Pydantic 请求/响应模型
db/          → 数据库连接层：异步引擎和会话管理
jobs/        → 定时任务层：APScheduler 调度
storage/     → 文件管理层：本地文件系统操作
mcp_server/  → MCP 协议层：Model Context Protocol 服务端
```

**推荐调用链（从外到内）**：
```
api → service → agent/tool → db/storage/external
```

**反模式（项目中严格遵守不这样做）**：
- 禁止在 API 路由层写业务逻辑
- 禁止在 Vue 组件中直接写 axios 请求
- 禁止 Tool 层直接抛出未处理异常

### 4.2 核心入口文件：`backend/main.py`

```python
# 生命周期：
@asynccontextmanager
async def lifespan(app: FastAPI):
    await init_db()        # 1. 数据库初始化（自动建表）
    await start_scheduler() # 2. 加载订阅任务到调度器
    yield
    shutdown_scheduler()   # 3. 优雅关闭
```

- 注册 6 个路由模块（chat, papers, library, subscriptions, traces, settings）
- CORS 中间件允许 localhost:5173（Vite 开发服务器）
- 应用启动时自动初始化数据库和调度器

### 4.3 服务启动流程

```
FastAPI 启动
  → lifespan 事件触发
  → init_db() 创建所有表
  → start_scheduler() 从数据库加载 enabled=true 的订阅任务
  → 每个任务注册为 APScheduler cron job
  → 到时间点后触发 run_daily_subscription()
```

---

## 5. Agent 编排系统（核心）

### 5.1 整体调度流程

Agent 编排系统的入口是 `backend/app/agent/orchestrator.py` 中的 `AgentOrchestrator` 类。

```
用户发消息 POST /api/chat
  → chat_service.handle_chat_message()
  → AgentOrchestrator.handle_chat(message, session_id)
  → 创建 Trace
  → 意图识别 (intent_classifier)
  → 判断是否走 ReAct 路径：
      ├── 配置了 USE_REACT_AGENT=true 且 intent=paper_search
      │   → ReactAgent.run() → 返回结果
      └── 否则走固定流程 (Fixed Pipeline)
          ├── intent=paper_search → _paper_search_flow()
          ├── intent=general_chat → 返回欢迎语
          └── 其他意图 → 不支持提示
```

### 5.2 双模式设计

Orchestrator 支持两种模式，通过环境变量 `USE_REACT_AGENT` 控制：

**模式一：固定流水线（Fixed Pipeline，默认）**
```python
# 文件：backend/app/agent/orchestrator.py, line 83-110
if intent == "paper_search":
    result = await self._paper_search_flow(intent_result, trace)
```

流程固定：
```
intent classify → query normalize → arxiv search → rerank → card summary → return
```

**模式二：ReAct Agent**
```python
# 文件：backend/app/agent/orchestrator.py, line 64-80
if self.use_react and intent == "paper_search":
    react_result = await self._react_agent.run(...)
```

流程由模型动态决策：
```
模型读取可用工具 → 选择 action → 执行 → 观察结果 → 再决策 → ... → final_answer
```

### 5.3 论文搜索固定流程详解

对应 `_paper_search_flow()` 方法 (orchestrator.py:112-204)：

```python
# Step 2: Query Normalization
raw_topic = entities.get("topic", "agent")  # 从用户消息提取
normalized_topic = normalize_query(raw_topic)  # 纠错 → "agent"
top_n = entities.get("top_n", 2)  # 提取用户要求的数量
candidate_k = entities.get("candidate_k", 20)  # 候选池大小
# ↓ trace: query_normalization

# Step 3: arXiv Search
search_result = await self.arxiv_tool.search(
    query=normalized_topic, max_results=candidate_k
)  # 调用 arXiv API 获取候选论文
# ↓ trace: arxiv_search

# Step 4: Rerank
ranked = await self.rerank_tool.rerank(
    query=normalized_topic, papers=papers, top_n=top_n
)  # 多因子排序：从20篇中选出Top 2
# ↓ trace: candidate_rerank

# Step 5: LLM Card Summary
for paper in ranked_papers:
    summary_result = await self.report_tool.generate_card_summary(
        paper=paper, query=normalized_topic
    )  # 用LLM为每篇论文生成中文摘要卡片
# ↓ trace: card_summary_generation

# Step 6: Build response → ChatResponse (包含 PaperCardItem 列表)
```

### 5.4 单例模式共享编排器

```python
# 文件：backend/app/agent/shared.py
from app.agent.orchestrator import AgentOrchestrator

orchestrator = AgentOrchestrator()
```

所有 API 和 Service 共用同一个 Orchestrator 实例，确保：
- trace_tool 一致
- ReAct Agent 状态统一
- 避免重复初始化 Tool 实例

---

## 6. ReAct Agent 设计（架构升级关键）

### 6.1 设计动机

**为什么需要 ReAct Agent？**

固定编排型 Agent 的缺点：
- 对于简单任务（"搜2篇agent论文"）走固定流程没问题
- 但对于复杂任务（"先搜论文，如果找到超过5篇就重排选出最好的，再给我推荐最值得读的那篇"）需要写大量 if/else
- 模型不能根据中间观察结果动态调整下一步

**为什么是"受控" ReAct？**

完全自由的 Agent 会导致：
- 模型可能调用不安全的工具（如搜索阶段偷偷全文解析）
- 容易产生无限循环
- 行为不可预测，调试困难

因此设计为 **Controlled ReAct Agent**：模型可以自主选择工具，但必须在 Tool Registry 的白名单内。

### 6.2 ReAct Agent 核心实现

文件：`backend/app/agent/react_agent.py`

#### 类结构

```python
class AgentState:  # 状态容器
    user_message: str       # 用户原始消息
    session_id: str         # 会话ID
    observations: list[dict] # 观察历史（最近4条）
    step_count: int         # 当前步数
    intent: str             # 意图

class ReactAgent:  # ReAct 执行器
    tool_registry: ToolRegistry  # 工具注册中心
    llm: LLMClient               # LLM 客户端
    trace_tool: TraceTool        # 追踪工具
    MAX_STEPS = 6                # 最大步数限制
```

#### 核心循环：`run()` 方法

```python
async def run(self, message, session_id, intent):
    # 1. 创建 Trace
    trace = self.trace_tool.create(task_type="react_agent", ...)

    # 2. 初始化状态
    state = AgentState(user_message=message, session_id=session_id)

    # 3. 获取当前 intent 允许的工具列表
    tools = self._get_allowed_tools(intent)  # 按 intent 过滤
    tool_schemas = [t.schema for t in tools]  # 转 OpenAI function schema

    # 4. ReAct 循环（最多 6 步）
    for step_idx in range(MAX_STEPS):
        # Plan: 让 LLM 根据当前状态和可用工具，决定下一步
        plan_result = await self.llm.plan_with_tools(
            user_message=message,
            state_summary=state.to_summary(),  # 最近4条观察的摘要
            tools=tool_schemas,
        )
        # plan_result = {
        #     "reasoning_summary": "用户想搜索论文...",
        #     "action": "arxiv_search",  或 "final_answer"
        #     "arguments": {"query": "agent", "max_results": 20}
        # }

        # 记录 Trace
        await self.trace_tool.log_step(...)

        # 如果是 final_answer，结束循环
        if plan_result["action"] == "final_answer":
            return self._build_final_response(...)

        # Validate: 检查工具是否在注册表中
        if plan_result["action"] not in self.tool_registry:
            state.add_observation({"success": False, "message": "Unknown tool"})
            continue  # 给模型一次机会重新选择

        # Execute: 通过 Tool Registry 调用工具
        obs = await self.tool_registry.call(
            name=plan_result["action"],
            arguments=plan_result["arguments"],
            context={"intent": intent},
        )

        # 记录 Trace
        await self.trace_tool.log_step(...)

        # 将观察结果加入状态
        state.add_observation(obs)

    # 5. 超过 max_steps → 终止
    return {"success": False, "message": "任务执行步骤过多，已停止"}
```

### 6.3 关键约束机制

**1. 工具白名单（按 intent 过滤）**
```python
# react_agent.py:167-172
def _get_allowed_tools(self, intent: str):
    return [
        t for t in self.tool_registry._tools.values()
        if intent in t.allowed_intents or "*" in t.allowed_intents
    ]
```
- 每个工具定义时声明 `allowed_intents`
- 模型只能看到和使用当前 intent 允许的工具
- 例如：`paper_search` 意图看不到 `pdf_parse_full_text` 工具

**2. max_steps 上限**
```python
MAX_STEPS = 6  # 超过6步自动终止
```

**3. 观察历史截断**
```python
# AgentState.to_summary()
for obs in self.observations[-4:]:  # 只保留最近4条
```

**4. 失败处理**
```python
if not obs.get("success"):
    continue  # 给模型一次重试机会，但不无限循环
```

### 6.4 LLM 规划决策：`plan_with_tools()`

对应 `llm_client.py:233-307`。这是 ReAct 的核心——让 LLM 根据当前状态和可用工具，输出下一个 action。

```python
async def plan_with_tools(self, user_message, state_summary, tools):
    system_prompt = f"""You are a controlled ReAct Agent for an arXiv paper assistant.

Available tools:
- arxiv_search: Search arXiv for papers matching a research topic query
- paper_rerank: Rerank candidate papers by semantic similarity...

Rules:
1. Choose ONE action from the available tools, or use "final_answer"
2. Provide a brief reasoning_summary (one sentence in Chinese)
3. Only use tools listed above. Do not invent tools.
4. Do not call expensive tools (parsing, downloading) during search.
5. max_steps per task is 6.

Current state: {state_summary}

Output exactly this JSON:
{{"reasoning_summary": "...", "action": "tool_name or final_answer", "arguments": {{...}}}}"""

    # 调用 LLM 的 chat_json 方法获取结构化 JSON
    result = await self.chat_json(messages=[...])
    return result
```

**关键设计点**：
- System Prompt 中明确列出当前可用工具及描述
- 强制输出 JSON（通过 `response_format: {"type": "json_object"}`）
- 不暴露完整思维链（只要求一句中文 reasoning_summary）
- rules 中包含业务规则（搜索阶段不调用 expensive 工具）

### 6.5 Bootstrap：工具注册

文件：`backend/app/agent/bootstrap.py`

```python
def create_tool_registry() -> ToolRegistry:
    registry = ToolRegistry()
    # 注册 5 个只读工具
    registry.register(ToolDefinition(
        name="arxiv_search",
        schema=ARXIV_SEARCH_SCHEMA,  # OpenAI function calling format
        handler=arxiv_tool.search,    # 实际的异步处理器
        allowed_intents=["paper_search", "subscription_run", "library_search", "*"],
        permission="read_only",
    ))
    # ... paper_rerank, paper_generate_card_summary, library_search_papers, trace_query
    return registry
```

每个工具注册时需要提供：
- `name`: 工具名（模型会使用这个名字）
- `schema`: OpenAI Function Calling 的 JSON Schema
- `handler`: 实际执行的异步函数
- `allowed_intents`: 允许在哪些意图下被调用
- `permission`: 权限等级

---

## 7. 意图识别系统

### 7.1 设计方案

```
小模型意图识别 + 关键词规则兜底
├── 优先：LLM 意图分类（当 llm_client.available）
│   └── 输出结构化 JSON: {intent, confidence, entities}
├── 兜底：关键词规则（当前 MVP 默认使用，因为未强制配置 LLM）
│   └── 正则匹配 + 实体提取
└── 最终兜底：general_chat
```

文件：`backend/app/agent/intent_classifier.py`

### 7.2 关键词规则实现

```python
# 规则示例
INTENT_RULES = [
    ("paper_search", [
        r"找|搜|推荐|论文|paper|arxiv|检索",
        r"(给我|帮我|想要|需要).*(找|搜|推荐|检索)",
    ]),
    ("paper_parse", [
        r"解析|精读|总结全文|深度阅读|全文解析",
    ]),
    ("subscription_create", [
        r"每天|每周|定时|早上|推送|订阅|发送",
    ]),
    # ... 共14种意图
]
```

**评分机制**：
- 每条规则匹配成功 +0.5 分，上限 1.0
- 选择最高分的意图
- 如果没有任何规则匹配 → `general_chat`

### 7.3 实体提取

```python
def _extract_entities(intent, message):
    # 提取 top_n: "找 2 篇" → 2
    top_n_match = re.search(r"(\d+)\s*[篇个]", message)

    # 提取 candidate_k: "从 50 篇" → 50
    cand_match = re.search(r"候选[池]?\s*(\d+)|从\s*(\d+)\s*篇", message)

    # 提取 topic: "关于 agent 的" → "agent"
    match = re.search(r"关于\s*(.+?)\s*[的论文]", message)

    # 提取订阅时间: "8点" → {"type":"daily","time":"08:00"}
    time_match = re.search(r"(\d{1,2})[点:：](\d{2})?", message)

    # 提取推送渠道
    if re.search(r"邮箱|邮件|email|mail", message):
        entities["channels"]["email"] = {"enabled": True}
    if re.search(r"飞书|feishu|lark", message):
        entities["channels"]["feishu"] = {"enabled": True}
```

### 7.4 Query Normalization（输入纠错）

```python
TYPO_MAP = {
    "agnet": "agent",       # 常见拼写错误
    "agentic": "agent",     # 同义词归一化
    "llm": "LLM",          # 缩写标准化
    "rag": "RAG",
    "gpt": "GPT",
    "nlp": "NLP",
    "cv": "CV",
    # ...
}

def normalize_query(query):
    words = query.split()
    normalized = []
    for word in words:
        if word.lower() in TYPO_MAP:
            normalized.append(TYPO_MAP[word.lower()])
        else:
            normalized.append(word)
    return " ".join(normalized)
```

---

## 8. Tool 层设计与 Tool Calling 方案

### 8.1 Tool 架构设计

```
┌─────────────────────────────────────┐
│          Tool Registry              │  ← 统一注册、校验、执行
│  register / get / list / call       │
│  ┌───────────┬───────────┐         │
│  │ Permission│ Business  │         │
│  │  Check    │ Rule Check│         │
│  └───────────┴───────────┘         │
└────────────┬────────────────────────┘
             ↓
┌──────────────────────────────────────┐
│         Tool Definitions             │
│  arxiv_search / paper_rerank /       │
│  card_summary / library_search /     │
│  trace_query / pdf_download / ...    │
└──────────────────────────────────────┘
```

### 8.2 ToolDefinition 数据结构

文件：`backend/app/agent/tool_schemas.py`

```python
@dataclass
class ToolDefinition:
    name: str                  # 工具名
    description: str           # 描述（给LLM看的）
    schema: dict               # OpenAI Function Calling JSON Schema
    handler: Callable          # 异步处理器函数
    allowed_intents: list[str] # 允许的意图范围
    permission: ToolPermission # 权限等级
    requires_confirmation: bool = False  # 是否需要二次确认

ToolPermission = Literal[
    "read_only",        # 只读（arxiv_search, trace_query）
    "write_safe",       # 安全写入（collect_paper, create_subscription）
    "write_dangerous",  # 危险写入（delete_paper）
    "expensive",        # 高成本（pdf_parse_full_text）
    "external_send",    # 外部发送（email_send, feishu_send）
]
```

### 8.3 ToolRegistry：核心容器

文件：`backend/app/agent/tool_registry.py`

```python
class ToolRegistry:
    def __init__(self):
        self._tools: dict[str, ToolDefinition] = {}

    def register(self, tool: ToolDefinition):  # 注册工具
    def get(self, name: str):                  # 按名称获取
    def list_tools(self, intent=None):         # 列出工具（可按intent过滤）
    def list_schemas(self, intent=None):       # 列出OpenAI Schema

    async def call(self, name, arguments, context) -> dict:
        # 1. 查找工具（不存在则返回 UNKNOWN_TOOL 错误）
        tool = self.get(name)

        # 2. 权限校验（_validate_permission）
        allowed, reason = self._validate_permission(tool, context)
        #   检查 BUSINESS_RULES: 搜索阶段禁止 pdf_parse_full_text 等
        if not allowed:
            return {"success": False, "error_code": "TOOL_NOT_ALLOWED", ...}

        # 3. 执行工具
        result = await tool.handler(**arguments)

        # 4. 异常处理（TypeError → INVALID_ARGUMENTS, Exception → TOOL_EXECUTION_FAILED）
        return result
```

### 8.4 业务规则校验

```python
BUSINESS_RULES = {
    "paper_search": {
        "deny_tools": {"pdf_parse_full_text", "paper_generate_deep_report"},
        "reason": "Search phase must not trigger full-text parsing",
    },
    "paper_collect": {
        "deny_tools": {"pdf_parse_full_text", "paper_generate_deep_report"},
        "reason": "Collect only downloads PDF, parsing is a separate action",
    },
}
```

这套规则确保：
- 搜索时绝不偷偷做全文解析
- 收藏只下载 PDF，不做解析
- 即使模型在 ReAct 中要求调用这些工具，也会被拒绝

### 8.5 OpenAI Function Schema 设计

每种工具都定义了符合 OpenAI Function Calling 规范的 JSON Schema：

```python
# 示例：arxiv_search 的 Schema
ARXIV_SEARCH_SCHEMA = {
    "type": "function",
    "function": {
        "name": "arxiv_search",
        "description": "Search arXiv for papers matching a research topic query...",
        "parameters": {
            "type": "object",
            "properties": {
                "query": {"type": "string", "description": "Research topic..."},
                "max_results": {"type": "integer", "description": "Max candidates...", "default": 20},
                "categories": {"type": "array", "items": {"type": "string"}, ...},
                "sort_by": {"type": "string", "enum": ["relevance", "submittedDate", ...]},
            },
            "required": ["query"],
        },
    },
}
```

### 8.6 LLM Client：多厂商支持

文件：`backend/app/tools/llm_client.py`

```python
PROVIDER_CONFIGS = {
    "openai": {"base_url": "https://api.openai.com/v1", ...},
    "deepseek": {"base_url": "https://api.deepseek.com/v1", ...},
    "qwen": {"base_url": "https://dashscope.aliyuncs.com/compatible-mode/v1", ...},
    "openai-compatible": {"base_url": "", ...},  # 自定义兼容接口
}

class LLMClient:
    available: bool  # 是否配置了 API Key
    provider: str    # 当前使用的厂商

    async def chat(messages, model, temperature)        # 普通对话
    async def chat_json(messages, model, temperature)   # 对话 + JSON 解析
    async def embed(texts, model)                       # 文本 Embedding
    async def chat_with_tools(messages, tools)          # Function Calling
    async def plan_with_tools(message, state, tools)    # ReAct 规划
```

**plan_with_tools** 是 ReAct 的核心方法：
- 构建 System Prompt（包含工具列表、规则约束）
- 强制 JSON 输出
- 返回 `{reasoning_summary, action, arguments}`

**所有 LLM 调用都是异步的**，使用 `httpx.AsyncClient`，确保不阻塞 FastAPI 事件循环。

### 8.7 各工具实现要点

#### ArxivTool (`tools/arxiv_tool.py`)
- 调用 arXiv 官方 API (`export.arxiv.org/api/query`)
- 解析 Atom XML 响应 → 提取结构化字段
- 支持分类过滤、日期过滤、排序方式
- 返回标准化的 paper dict 列表

#### RerankTool (`tools/rerank_tool.py`)
- 多因子评分（详见第11章）
- 综合语义相似度、关键词匹配、新鲜度、用户偏好

#### ReportTool (`tools/report_tool.py`)
- 双重策略：LLM 优先 + 正则兜底
- 论文卡片摘要（仅基于 title + abstract）
- 深度精读报告（11 章节 Markdown）

#### PdfTool (`tools/pdf_tool.py`)
- 使用 httpx 下载 PDF
- 使用 PyMuPDF (fitz) 解析文本
- 启发式章节提取（Abstract / Introduction / Method / Experiment / Conclusion）
- 生成 parsed.md

#### LibraryTool (`tools/library_tool.py`)
- 本地论文库 CRUD
- Soft delete（默认）+ Hard delete（可选）
- 支持按关键词/状态/标签搜索
- 与 filesystem 同步维护 metadata.json

#### TraceTool (`tools/trace_tool.py`)
- 创建 Trace（唯一 trace_id 格式：`trace_{日期}_{时间}_{uuid6位}`）
- 记录 Step（每个 step 对应一个工具调用）
- 查询/统计 Trace

---

## 9. Skill 设计（Skill-as-Tool 模式）

### 9.1 设计动机

不希望 ReAct Agent 直接自由拼接底层原子工具。原因：
1. 模型可能拼接出不符合业务规则的调用链
2. 步骤过多会导致 max_steps 超限
3. 容易跳过关键步骤（如 trace 记录）

解决方案：**把经过验证的稳定工作流包装成 Skill，作为一种高级 Tool 暴露给 Agent**。

```
底层原子工具（模型不能自由组合）：
  arxiv_search → paper_rerank → card_summary

包装成 Skill（模型可以安全调用）：
  paper_search_card_skill（内部固定执行上述三步）
```

### 9.2 Skill 注册中心

文件：`backend/app/agent/skill_registry.py`

```python
@dataclass
class SkillDefinition:
    name: str
    description: str
    schema: dict           # OpenAI Function Schema
    handler: Callable      # 异步处理器
    allowed_intents: list[str]
    permission: str

class SkillRegistry:
    def register(self, skill: SkillDefinition)
    def get(self, name: str) -> SkillDefinition
    def list_schemas(self, intent=None) -> list[dict]
    async def call(self, name, arguments) -> dict
```

### 9.3 已实现的 Skill

**1. paper_search_card_skill**
文件：`backend/app/agent/skills/paper_search_card_skill.py`

内部固定执行：
```
intent classify → query normalize → arxiv search → rerank → card summary
```

OpenAI Schema 参数：
```json
{
  "name": "paper_search_card_skill",
  "parameters": {
    "user_message": "原始用户消息",
    "topic": "研究主题",
    "top_n": 2,
    "candidate_k": 20
  }
}
```

**2. trace_diagnosis_skill**
文件：`backend/app/agent/skills/trace_diagnosis_skill.py`

内部固定执行：
```
trace.query → (如果有失败) → 分析失败原因 → 返回诊断建议
```

诊断逻辑 (`_suggest_fix`)：
```python
def _suggest_fix(task_type, error_message):
    if "429" in msg or "rate" in msg:
        return "arXiv API 限流，请稍后重试"
    if "timeout" in msg:
        return "外部服务超时，请检查网络连接"
    if "pdf" in msg:
        return "PDF 下载或解析失败，请检查 arXiv PDF 链接"
    # ...
```

---

## 10. MCP Server 设计

### 10.1 什么是 MCP

Model Context Protocol (MCP) 是 Anthropic 发布的开放协议，用于标准化 AI 模型与外部工具/数据源的交互方式。MCP 采用 **JSON-RPC 2.0 over stdio** 协议。

### 10.2 项目中的 MCP 定位

本项目实现了 **MCP Server** 角色，把论文助手的能力暴露给外部 AI 客户端（如 Claude Desktop、Cursor 等）：

```
外部 AI (Claude Desktop/Cursor)
    │
    │ MCP Protocol (JSON-RPC over stdio)
    ↓
本项目的 MCP Server (backend/mcp_server/server.py)
    │
    ├── Tools: arxiv.search_papers / library.search_papers / trace.query
    ├── Resources: library://paper/{id}/report / metadata / parsed
    └── Prompts: paper/card_summary_zh
    │
    ↓ 复用已有业务层
backend/app/tools/  (ArxivTool, LibraryTool, TraceTool)
```

### 10.3 MCP Server 核心架构

文件：`backend/mcp_server/server.py`

支持 6 种 JSON-RPC 方法：

| 方法 | 作用 | 实现文件 |
|------|------|----------|
| `initialize` | 握手，返回协议版本和能力列表 | server.py |
| `tools/list` | 列出所有可用工具及 Schema | 各 tools/*.py |
| `tools/call` | 调用指定的 MCP 工具 | 各 tools/*.py |
| `resources/list` | 列出所有可访问资源 URI | 各 resources/*.py |
| `resources/read` | 读取指定 URI 的资源内容 | 各 resources/*.py |
| `prompts/list` | 列出所有可用的 Prompt 模板 | prompts/*.py |
| `prompts/get` | 获取指定 Prompt 模板 | prompts/*.py |

### 10.4 MCP Tools（暴露的能力）

文件：`backend/mcp_server/tools/`

| 工具名 | 功能 | 对应后端 Tool |
|--------|------|--------------|
| `arxiv.search_papers` | 搜索 arXiv 论文 | `ArxivTool.search()` |
| `arxiv.get_paper_metadata` | 获取单篇论文元数据 | `ArxivTool.get_paper_metadata()` |
| `library.search_papers` | 搜索本地库论文 | `LibraryTool.search_papers()` |
| `library.get_paper` | 获取本地论文详情 | `LibraryTool.get_paper()` |
| `library.get_report` | 获取论文精读报告 | `LibraryTool.get_report()` |
| `trace.query` | 查询任务追踪 | `TraceTool.query()` |
| `trace.get` | 获取具体追踪详情 | `TraceTool.get()` |

### 10.5 MCP Resources（暴露的数据）

文件：`backend/mcp_server/resources/`

资源使用 URI 寻址：
- `library://paper/{arxiv_id}/report` → 论文精读报告 Markdown
- `library://paper/{arxiv_id}/metadata` → 论文元数据 JSON
- `library://paper/{arxiv_id}/parsed` → 解析全文 Markdown
- `trace://{trace_id}` → Trace 详情 JSON

### 10.6 设计原则

**关键原则：MCP 层不复制业务逻辑**

```python
# MCP tools 直接复用已有 backend/app/tools/
async def call_arxiv_tool(name, arguments):
    from app.tools.arxiv_tool import ArxivTool  # 复用

    tool = ArxivTool()
    result = await tool.search(...)
    await tool.close()
    return {"content": [{"type": "text", "text": json.dumps(result)}]}
```

这是非常干净的架构设计：
- MCP Server 是薄适配层
- 所有业务逻辑在 `tools/` 和 `services/` 中
- 避免代码重复

### 10.7 stdio 传输协议

```python
def main():
    """从 stdin 读取 JSON-RPC 请求，向 stdout 写 JSON-RPC 响应"""
    for line in sys.stdin:
        request = json.loads(line)
        response = process_request(request)
        sys.stdout.write(json.dumps(response) + "\n")
        sys.stdout.flush()
```

响应格式：
```json
{"jsonrpc": "2.0", "id": 1, "result": {"tools": [...]}}
// 或错误
{"jsonrpc": "2.0", "id": 1, "error": {"code": -32601, "message": "Method not found"}}
```

---

## 11. 论文检索与重排（RAG + 多因子排序）

### 11.1 完整检索流程

```
用户 query "agent reinforcement learning"
        ↓
  Query Normalization（纠错 + 标准化）
        ↓
  arXiv API 检索（初筛，candidate_k = 20篇）
        ↓
  多因子重排（Rerank）
  ┌─────────────────────────────────────────┐
  │ 1. Semantic Similarity (Embedding/TF-IDF) │ ← 权重 45%
  │ 2. Keyword Match Score                    │ ← 权重 20%
  │ 3. Recency Score                         │ ← 权重 15%
  │ 4. User Preference Score                 │ ← 权重 10%
  │ 5. Interaction (semantic × recency)       │ ← 权重 10%
  └─────────────────────────────────────────┘
        ↓
  Top N 论文 (按 final_score 降序)
        ↓
  LLM 生成中文论文卡片摘要
```

### 11.2 评分公式详解

文件：`backend/app/tools/rerank_tool.py`, 行 58-64

```python
final_score = (
    0.45 * semantic_score          # 语义相似度（核心权重）
    + 0.20 * keyword_score         # 关键词匹配
    + 0.15 * recency_score         # 时间新鲜度
    + 0.10 * pref_score            # 用户偏好匹配
    + 0.10 * (semantic_score * recency_score)  # 交互项
)
```

**交互项的设计意图**：让"语义相似且发布时间新"的论文获得额外加分。如果仅用线性加权，一篇语义相关但很旧的论文和一篇语义一般但很新的论文得分可能相近，但实际上用户更需要"最新的相关论文"。交互项解决了这个问题。

### 11.3 各评分因子实现

**语义相似度 (0.45)**：
```python
# rerank_tool.py:92-121
async def _semantic_scores(query, papers):
    try:
        # 优先使用 LLM Embedding API
        ranked, method = await self.embedding_tool.rank_by_similarity(
            query=query, papers=papers, top_n=len(papers)
        )
        return scores, "LLM"  # 使用 LLM embedding
    except Exception:
        # 兜底：纯 TF-IDF 余弦相似度
        return keyword_scores, "keyword"
```

**关键词匹配 (0.20)**：
```python
# rerank_tool.py:131-136
def _keyword_match_score(keywords, title, abstract):
    text = title + " " + abstract
    matches = sum(1 for kw in keywords if kw.lower() in text)
    return min(1.0, matches / max(len(keywords), 1))
```
关键词提取时过滤了中英文停用词（"关于"、"给我"、"找"、"篇"、"the"、"a"、"in"等）。

**新鲜度评分 (0.15)**：
```python
# rerank_tool.py:138-158
def _recency_score(published_date):
    days_ago = (now - pub_date).days
    if days_ago <= 7:    return 1.0   # 一周内
    elif days_ago <= 30:  return 0.9   # 一月内
    elif days_ago <= 90:  return 0.7   # 三个月内
    elif days_ago <= 180: return 0.5   # 半年内
    elif days_ago <= 365: return 0.3   # 一年内
    else:                 return 0.1   # 超过一年
```

**用户偏好 (0.10)**：
```python
# rerank_tool.py:51-55
if user_preferences:
    pref_cats = user_preferences.get("preferred_categories", [])
    matched_cats = set(categories) & set(pref_cats)
    pref_score = min(1.0, 0.5 + len(matched_cats) * 0.25)
```
匹配越多偏好分类，得分越高。

### 11.4 RAG 设计

本项目的 RAG（检索增强生成）体现在：

1. **R（检索）**：arXiv API 初筛 20 篇候选
2. **A（增强）**：多因子重排，选出最相关的 Top N 篇
3. **G（生成）**：LLM 基于论文 title + abstract 生成中文卡片摘要

与标准 RAG 的区别：
- 标准 RAG 用向量数据库做相似搜索，本项目用 arXiv API 做初筛
- 标准 RAG 通常单靠向量相似度排序，本项目综合了语义、关键词、时间、偏好等多维度

---

## 12. Embedding 与语义相似度

### 12.1 双重策略设计

文件：`backend/app/tools/embedding_tool.py`

```python
class EmbeddingTool:
    async def rank_by_similarity(query, papers, top_n):
        # 策略1：LLM Embedding API（需要配置 API Key）
        if llm_client.available:
            result = await self._llm_rerank(query, doc_texts, papers, top_n)
            if result["success"]:
                return result["papers"], "LLM"

        # 策略2：纯 Python TF-IDF（零依赖，始终可用）
        return self._tfidf_rerank(query, doc_texts, papers, top_n), "TF-IDF"
```

### 12.2 LLM Embedding 路径

```python
# embedding_tool.py:112-140
async def _llm_rerank(query, doc_texts, papers, top_n):
    all_texts = [query] + doc_texts  # query 放在第一位
    embed_result = await llm_client.embed(all_texts)

    embeddings = embed_result["embeddings"]
    query_embedding = embeddings[0]      # query 的向量
    doc_embeddings = embeddings[1:]      # 各论文的向量

    # 计算余弦相似度
    for i, (paper, doc_emb) in enumerate(zip(papers, doc_embeddings)):
        score = cosine_similarity(query_embedding, doc_emb)
        paper["_rerank_score"] = round(score, 4)

    return sorted by score descending [:top_n]
```

### 12.3 TF-IDF 兜底路径（纯 Python 实现）

```python
# embedding_tool.py:14-71
def _tokenize(text):    # 分词（支持中英文）
    tokens = re.findall(r"[a-zA-Z0-9一-鿿]+", text.lower())
    return [t for t in tokens if len(t) > 1]

def _build_tfidf_vectors(query, documents):
    # 构建词汇表 → 计算 IDF → 计算 TF-IDF 向量 → L2 归一化
    ...

def cosine_similarity(a, b):
    dot = sum(x * y for x, y in zip(a, b))
    norm_a = math.sqrt(sum(x * x for x in a))
    norm_b = math.sqrt(sum(x * x for x in b))
    return dot / (norm_a * norm_b)
```

这是完全自实现的 TF-IDF + 余弦相似度，**不依赖任何外部库**（sklearn、numpy 等），纯 Python 标准库。中文分词使用 Unicode 范围 `一-鿿`。

---

## 13. Memory 记忆系统设计

### 13.1 Memory 类型

项目定义了五类 Memory，各有不同用途：

```
1. UserPreferenceMemory  → 存储在 user_preferences 表
2. PaperBehaviorMemory   → 存储在 papers 表的行为字段
3. LocalLibraryMemory    → 存储在 papers 表 + 本地文件系统
4. SubscriptionMemory    → 存储在 subscriptions 表 + subscription_runs 表
5. TraceMemory           → 存储在 tasks 表 + task_steps 表
```

### 13.2 用户偏好 Memory（UserPreferenceMemory）

存储用户的系统配置：
```json
{
  "default_candidate_k": 20,
  "default_top_n": 2,
  "preferred_topics": ["RAG", "Agent", "LLM"],
  "preferred_categories": ["cs.CL", "cs.AI", "cs.LG"],
  "summary_language": "zh-CN",
  "summary_style": "technical",
  "auto_parse_full_text": false
}
```

对应数据表：
```sql
CREATE TABLE user_preferences (
    key TEXT UNIQUE NOT NULL,
    value TEXT NOT NULL,
    updated_at TEXT
);
```

### 13.3 论文行为 Memory（PaperBehaviorMemory）

记录用户与论文的交互行为：
- 收藏过哪些论文
- 解析过哪些论文
- 删除过哪些论文
- 不感兴趣的论文
- 经常搜索的主题

体现在 `papers` 表的字段中：`source`、`status`、`has_pdf`、`has_report`

### 13.4 本地文献 Memory（LocalLibraryMemory）

本地文件系统中的论文数据本身是长期记忆：
```
paper_library/papers/{arxiv_id}/
  ├── paper.pdf       # PDF 原文
  ├── metadata.json   # 元数据
  ├── parsed.md       # 解析全文
  └── report.md       # 精读报告
```

### 13.5 Memory 使用原则

重要设计决策：**不要把全部 Memory 都塞进 Prompt**。只放当前任务必要的信息：
- 当前 query
- 用户偏好（top_n, candidate_k, preferred_categories）
- 候选论文的 title/abstract/introduction
- 本次任务上下文

这样做的好处：
- 减少 Token 消耗
- 提高 LLM 响应质量（不被无关信息干扰）
- 保护用户隐私（不暴露完整历史）

---

## 14. Prompt 工程

### 14.1 Prompt 模板体系

文件：`backend/app/agent/prompts.py`

项目定义了 3 套 System Prompt + 多个动态构建模板：

#### 论文卡片摘要 Prompt

```python
CARD_SUMMARY_SYSTEM = """你是一个 arXiv 论文摘要助手。你的任务是根据论文的标题和摘要生成中文论文卡片。

规则：
1. 你只能根据给定的内容总结，不能编造论文正文不存在的信息
2. 如果给定内容不足，请说明"不确定"
3. 输出必须是严格的 JSON 格式
4. 输出语言为中文

输出格式：
{
  "summary": "一句话总结论文内容",
  "core_problem": "论文要解决的核心问题",
  "method": "论文提出的方法",
  "result": "论文的主要结果",
  "recommendation_reason": "推荐给用户的理由"
}"""
```

Prompt 构建：
```python
def build_card_summary_prompt(title, abstract, introduction="", query=""):
    return f"""论文标题：{title}
摘要：{abstract}
{intro if intro else ""}
{query if query else ""}
请基于以上内容生成中文论文卡片。"""
```

#### 深度精读报告 Prompt

```python
DEEP_REPORT_SYSTEM = """你是一个学术论文精读助手。请基于论文全文内容生成中文精读报告。

报告必须包含以下章节：
1. 论文基本信息
2. 一句话总结
3. 研究背景
4. 核心问题
5. 方法详解
6. 关键创新点
7. 实验设计（数据集、Baseline、评价指标、实验设置）
8. 主要结果
9. 局限性
10. 对用户研究方向的价值
11. 可复现性判断（是否有代码、数据、超参数；复现难度：高/中/低）

使用 Markdown 格式输出。"""
```

#### ReAct Agent 规划 Prompt

```python
# 在 llm_client.py plan_with_tools() 中动态构建
system_prompt = f"""You are a controlled ReAct Agent for an arXiv paper assistant.

Available tools:
{tool_descriptions}

Rules:
1. Choose ONE action from the available tools, or use "final_answer"
2. Provide a brief reasoning_summary (one sentence in Chinese)
3. Only use tools listed above. Do not invent tools.
4. Do not call expensive tools (parsing, downloading) during search.
5. If the task is simple, prefer final_answer.
6. max_steps per task is 6.

Current state: {state_summary}

Output exactly this JSON:
{{"reasoning_summary": "...", "action": "tool_name or final_answer", "arguments": {{...}}}}"""
```

### 14.2 Prompt 设计原则

1. **内容边界守门**：Prompt 明确约束"只能根据给定内容总结，不能编造"
2. **结构化输出**：所有 LLM 调用都要求 JSON/Markdown 格式
3. **含业务规则**：ReAct Prompt 中直接嵌入业务规则（如不调用 expensive 工具）
4. **中文优先**：论文卡片和精读报告都是中文输出
5. **不超过上下文限制**：全文解析报告限制输入 8000 字符

---

## 15. 可观测性（Trace-first Observability）

### 15.1 为什么 Trace 是第一优先级

这是本项目最重要的设计决策之一。在 ReAct Agent 模式下，如果每一步决策不可见，Agent 就是黑盒。因此采用 **Trace-first Observability**：

> 每一次 reasoning_summary、action、arguments、observation 都会进入 trace。既保留 Agent 的灵活性，又避免"模型到底做了什么"不可知的问题。

### 15.2 Trace 数据模型

两个核心表：

**tasks 表**（一次任务的完整记录）：
```sql
CREATE TABLE tasks (
    trace_id TEXT UNIQUE,    -- 唯一标识: trace_20260520_120000_a1b2c3
    task_type TEXT,          -- chat / paper_search / paper_collect / paper_parse / subscription_run
    user_input TEXT,         -- 用户输入
    summary TEXT,            -- 任务摘要
    tags TEXT,               -- 逗号分隔的标签
    status TEXT,             -- running / success / failed
    started_at TEXT,
    ended_at TEXT,
    duration_ms INTEGER,
    error_message TEXT
);
```

**task_steps 表**（任务中每个步骤的记录）：
```sql
CREATE TABLE task_steps (
    trace_id TEXT,            -- 关联到 tasks
    step_name TEXT,           -- intent_recognition / arxiv_search / react_step_1 ...
    tool_name TEXT,           -- 调用的工具名
    reasoning_summary TEXT,    -- ReAct 的决策推理（不存完整思维链）
    input_summary TEXT,       -- 输入摘要
    output_summary TEXT,      -- 输出摘要
    status TEXT,              -- success / failed / skipped / running
    started_at TEXT,
    ended_at TEXT,
    duration_ms INTEGER,
    error_message TEXT
);
```

### 15.3 Trace 生成机制

文件：`backend/app/tools/trace_tool.py`

```python
class TraceTool:
    def create(task_type, user_input, tags) -> Trace:
        # 生成唯一 trace_id: trace_日期_时间_uuid6位
        trace_id = f"trace_{now:%Y%m%d_%H%M%S}_{uuid4().hex[:6]}"

    async def log_step(trace_id, step_name, ...) -> dict:
        # 立即写入数据库（不是等任务结束才批量写入）

    async def complete(trace_id, status, ...) -> Trace:
        # 更新任务状态、结束时间、计算耗时
```

**设计亮点**：每个 step 都是**立即写入数据库**的，不是等任务结束才批量写。这意味着即使任务中途崩溃，也能看到已完成的步骤。

### 15.4 典型 Trace 示例

一次成功的论文搜索：
```
Trace ID: trace_20260520_120000_a1b2c3
Task Type: paper_search
User Input: "给我找 2 篇关于 agent 的论文"
Tags: [paper_search, chat, topic:agent, top_n:2, candidate_k:20]
Status: success
Duration: 4200ms

Steps:
  1. intent_recognition       → success
     output: intent=paper_search, confidence=0.98
  2. query_normalization       → success
     output: normalized_topic=agent, candidate_k=20
  3. arxiv_search              → success
     output: found=18 papers
  4. candidate_rerank          → success
     output: selected=2 papers
  5. card_summary_generation   → success
     output: generated=2 cards
```

一次失败的订阅运行：
```
Trace ID: trace_20260520_080000_x9y8z7
Task Type: subscription_run
Status: failed
Error: SMTP connection timeout

Steps:
  1. arxiv_search              → success (found=15)
  2. candidate_rerank          → success (selected=2)
  3. auto_collect              → success (collected=2)
  4. email_send                → failed (SMTP timeout)
```

### 15.5 前端 Trace 展示

TraceView.vue 提供完整的查询和展示：
- 按关键词/任务类型/状态/标签搜索
- 列表展示：trace_id、任务类型、状态、耗时、错误信息
- 展开查看 TraceTimeline（可视化步骤时间线）
- 每个 step 显示：step_name、tool_name、reasoning_summary、input/output、错误信息

```
成功 ──●── Check 图标 (绿色)
失败 ──●── Close 图标 (红色)
跳过 ──●── Remove 图标 (灰色)
运行中 ●── 蓝色脉冲

连接线 ──┬── (竖线连接各步骤)
        ├──
        └──
```

---

## 16. 订阅任务与定时调度

### 16.1 系统架构

```
┌────────────────────────────────────────┐
│         APScheduler (AsyncIOScheduler) │
│  启动时加载所有 enabled=true 的订阅     │
│  每个订阅注册为一个 cron job            │
└────────────┬───────────────────────────┘
             │ 到达设定时间
             ↓
┌────────────────────────────────────────┐
│    run_daily_subscription(sub_id)      │
│    1. 获取订阅配置（topics, top_n等）   │
│    2. 对每个 topic 搜索 arXiv            │
│    3. 去重 + 多因子重排                 │
│    4. 自动收藏论文（下载PDF + 入库）     │
│    5. 构建每日精选 Markdown             │
│    6. 发送邮件（可选）                  │
│    7. 发送飞书（可选）                  │
│    8. 记录运行结果（subscription_runs）  │
│    9. 每步写入 Trace                   │
└────────────────────────────────────────┘
```

### 16.2 调度器实现

文件：`backend/app/jobs/scheduler.py`

```python
scheduler = AsyncIOScheduler()
_job_ids: dict[int, str] = {}  # subscription_id → job_id

async def start_scheduler():
    scheduler.start()
    # 从数据库加载所有启用的订阅
    sub_tool = SubscriptionTool()
    result = await sub_tool.list(enabled=True)
    for sub in result["subscriptions"]:
        add_subscription_job(sub["id"], sub["cron_expr"], sub["timezone"])

def add_subscription_job(subscription_id, cron_expr, timezone):
    # 解析 cron 表达式 (如 "0 8 * * *" → 每天早上8点)
    trigger = CronTrigger(minute=..., hour=..., timezone=timezone)
    scheduler.add_job(_run_subscription, trigger=trigger, args=[subscription_id])
```

### 16.3 订阅执行流程

文件：`backend/app/jobs/daily_subscription_job.py`

```python
async def run_daily_subscription(subscription_id, dry_run=False):
    # 1. 获取订阅配置
    sub = await sub_tool.get(subscription_id)
    topics = sub["topics"]        # ["RAG", "Agent", "LLM"]
    candidate_k = sub["candidate_k"]  # 20
    top_n = sub["top_n"]              # 2

    # 2. 对每个 topic 搜索 arXiv
    for topic in topics:
        papers = await arxiv_tool.search(topic, max_results=candidate_k)
        all_papers.extend(papers)

    # 3. 去重
    unique_papers = deduplicate_by_arxiv_id(all_papers)

    # 4. 多因子重排（合并所有 topic 的 query）
    ranked = await rerank_tool.rerank(
        query=" ".join(topics), papers=unique_papers, top_n=top_n
    )
    selected_papers = ranked["ranked_papers"]

    # 5. 自动收藏（下载PDF + 入库）
    if not sub["auto_parse_full_text"]:
        for paper in selected_papers:
            dl_result = await pdf_tool.download_pdf(arxiv_id, pdf_url)
            await library_tool.add_paper(paper, files={"pdf_path": ...})

    # 6. 构建每日精选 Markdown
    digest = _build_daily_digest(sub["name"], selected_papers, run_date)

    # 7. 发送通知
    if sub["email_enabled"]:
        await notify_tool.send_email(to=..., subject=..., content=digest)
    if sub["feishu_enabled"]:
        await notify_tool.send_feishu(webhook=..., title=..., content=digest)

    # 8. 记录运行结果
    await sub_tool.record_run(subscription_id, ..., selected_papers, ...)
```

### 16.4 关键业务规则

- **默认不全文解析**：`auto_parse_full_text = false`，只基于 title+abstract 生成每日精选
- **自动收藏**：被选中的论文自动下载 PDF 并入库
- **dry_run 模式**：支持试运行，不真正收藏和发送
- **失败不影响入库**：邮件/飞书发送失败只会标记，不会回滚论文收藏

---

## 17. 前端架构分析

### 17.1 前端调用链

严格按照三层架构：

```
View/Component    →    Pinia Store    →    API Layer    →    Backend
(UI展示+事件)       (状态管理+逻辑)      (HTTP请求封装)     (FastAPI)
```

**反模式（严格遵守不这样做）**：
- 不在组件中直接写 axios 请求
- 不在 store 中直接操作 DOM

### 17.2 关键交互流程

#### 聊天搜索流程
```
ChatInput.vue  → emit('send', message)
ChatWindow.vue → chatStore.sendChatMessage(message)
chatStore.ts   → chatApi.sendMessage(request) → POST /api/chat
ChatWindow.vue → 收到 ChatResponse → 展示 MessageBubble (含 PaperCard)
```

#### 论文收藏流程
```
PaperCard.vue  → emit('collect', paper)
ChatWindow.vue → paperApi.collectPaper(arxivId, paper) → POST /api/papers/{id}/collect
ChatWindow.vue → ElMessage.success("论文已收藏") / ElMessage.error("收藏失败")
```

#### 论文解析流程
```
PaperCard.vue  → emit('parse', paper)
ChatWindow.vue → paperApi.parsePaper(arxivId) → POST /api/papers/{id}/parse
ChatWindow.vue → ElMessage.success({message: "解析完成！trace: xxx"})
               → 用户可以跳转到 /report/{arxivId} 查看解析报告
```

### 17.3 Axios 拦截器设计

文件：`frontend/src/api/http.ts`

**请求拦截器**：自动将前端 camelCase 转为后端 snake_case
```typescript
http.interceptors.request.use((config) => {
  if (config.data && typeof config.data === 'object') {
    config.data = camelToSnake(config.data)  // arxivId → arxiv_id
  }
  return config
})
```

**响应拦截器**：统一错误日志
```typescript
http.interceptors.response.use(
  (response) => response,
  (error) => {
    console.error('API Error:', error.message)
    return Promise.reject(error)
  }
)
```

### 17.4 状态管理（Pinia Stores）

| Store | 管理状态 | 核心方法 |
|-------|---------|---------|
| `chatStore` | messages, loading, sessionId | `sendChatMessage()` |
| `libraryStore` | papers, total, loading | `fetchPapers()`, `deletePaper()` |
| `subscriptionStore` | subscriptions, loading | `fetchSubscriptions()`, `create/update/delete` |
| `traceStore` | traces, total, currentTrace | `fetchTraces()`, `fetchTraceDetail()` |
| `settingsStore` | preferences | `fetchSettings()`, `updateSettings()` |

### 17.5 组件设计

**layout/**
- `AppLayout.vue`：主布局（侧边栏 + 内容区）
- `SideNav.vue`：导航菜单

**chat/**
- `ChatWindow.vue`：聊天主容器（消息列表 + 输入框）
- `ChatInput.vue`：输入框组件（含发送按钮）
- `MessageBubble.vue`：消息气泡（区分 user/assistant）
- `PaperCard.vue`：论文卡片组件（标题/摘要/操作按钮）

**trace/**
- `TraceTimeline.vue`：步骤时间线组件（可视化 trace steps）

**common/**
- `MarkdownViewer.vue`：Markdown 渲染组件
- `LoadingState.vue`：加载状态
- `EmptyState.vue`：空状态

---

## 18. 数据库设计

### 18.1 数据表总览

| 表名 | 用途 | 关键字段 |
|------|------|---------|
| `papers` | 论文主表 | arxiv_id, title, status, has_pdf, has_report |
| `paper_files` | 论文文件路径 | arxiv_id, pdf_path, parsed_path, report_path |
| `paper_summaries` | 论文摘要 | arxiv_id, short_summary, core_problem, method_summary |
| `paper_tags` | 论文标签 | arxiv_id, tag |
| `subscriptions` | 订阅任务 | name, topics, cron_expr, email_enabled, feishu_enabled |
| `subscription_runs` | 订阅运行记录 | subscription_id, run_date, selected_papers, status |
| `tasks` | 任务追踪 | trace_id, task_type, status, duration_ms |
| `task_steps` | 任务步骤 | trace_id, step_name, tool_name, status, error_message |
| `user_preferences` | 用户偏好 | key, value |

### 18.2 论文状态机

```
                         ┌──────────┐
                         │  新建论文  │
                         └────┬─────┘
                              ↓ 收藏成功
                         ┌──────────┐
                ┌────────│ collected │
                │        └────┬─────┘
                │             ↓ 解析成功
                │        ┌──────────┐
                │        │  parsed   │
                │        └────┬─────┘
                │             ↓ 删除
                │        ┌──────────┐
                └───────→│ deleted   │
                         └──────────┘

状态变更规则：
  收藏成功 → status=collected, has_pdf=true, has_parsed_doc=false, has_report=false
  解析成功 → status=parsed,   has_pdf=true, has_parsed_doc=true,  has_report=true
  软删除   → status=deleted   (数据保留，文件不删除)
  解析失败 → status=failed    (PDF 已下载，不删除)
```

### 18.3 数据库引擎

```python
# backend/app/db/database.py
engine = create_async_engine(
    settings.database_url,  # 默认: sqlite+aiosqlite:///./data/library.db
    echo=False
)
async_session = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)
```

- MVP 使用 SQLite + aiosqlite（零配置，适合本地运行）
- 正式版可切换为 PostgreSQL（修改 DATABASE_URL 环境变量即可）
- AsyncSession 确保 FastAPI 异步模型下不阻塞事件循环

---

## 19. 安全设计原则

### 19.1 敏感信息保护

```text
✅ 好的做法：
- API Key 使用环境变量（settings.llm_api_key，从 .env 读取）
- 飞书 webhook 不写明文日志
- SMTP 密码使用环境变量
- Trace 中只记录摘要，不记录完整 Prompt

❌ 不会做的事：
- 把 API Key 写死在代码里
- 把飞书 webhook 写进日志
- 在 trace 中记录完整 PDF 正文
- 暴露完整思维链到前端
```

### 19.2 操作安全

| 规则 | 实现方式 |
|------|---------|
| 删除默认 soft delete | `LibraryTool.delete_paper(arxiv_id, mode="soft")` |
| hard delete 需确认 | API 参数 `?hard=true`，前端二次确认 |
| 搜索阶段禁止全文解析 | `BUSINESS_RULES` + `allowed_intents` 限制 |
| 收藏和解析分离 | 不同的 API 端点，不同的 Tool 权限 |
| 订阅默认不全文解析 | `auto_parse_full_text` 默认为 `false` |

### 19.3 错误处理

所有 API 统一返回格式：
```json
{
  "success": false,
  "trace_id": "trace_xxx",
  "message": "用户友好的错误描述",
  "error_code": "ARXIV_SEARCH_FAILED",
  "detail": "技术错误详情（不暴露敏感信息）"
}
```

---

## 20. 项目亮点总结（面试重点）

以下是面试中可以重点强调的项目亮点：

### 架构设计亮点

1. **从固定编排演进为受控 ReAct Agent**
   - 不是简单的 CRUD 加 API 调用
   - 模型可以在安全边界内自主选择下一步工具
   - 每一步经历"白名单→权限→业务规则→Trace"四道检查

2. **Tool Registry + Skill-as-Tool 模式**
   - 底层 Tool 负责原子能力，Skill 负责稳定工作流
   - 对模型来说，Skill 也是 Tool，避免自由拼接
   - OpenAI Function Calling Schema 标准化

3. **MCP Server 设计**
   - 将项目能力标准化暴露为 MCP Server
   - JSON-RPC 2.0 over stdio
   - Tools/Resources/Prompts 三层能力
   - 薄适配层（复用已有业务代码，不重复实现）

### 工程实践亮点

4. **Trace-first Observability**
   - 每一步操作都有 trace_id
   - task + task_steps 双表设计
   - 失败步骤有 reasoning_summary + action + observation
   - 前端 Timeline 可视化展示

5. **多因子论文重排算法**
   - 语义相似度（Embedding/TF-IDF）+ 关键词 + 新鲜度 + 用户偏好 + 交互项
   - LLM Embedding API 优先，纯 Python TF-IDF 兜底
   - 权重可调，适配不同场景

6. **双重策略设计（LLM + 兜底）**
   - 卡片摘要：LLM → 正则兜底
   - 语义排序：Embedding API → TF-IDF 兜底
   - 意图识别：LLM → 关键词规则兜底
   - 保证系统在任何配置下都能运行

7. **严格的业务规则约束**
   - 搜索阶段不全文解析
   - 收藏和解析分离
   - 删除默认 soft delete
   - 每日订阅默认不全文解析
   - 所有规则在 Tool Registry 层硬编码执行

### 代码质量亮点

8. **清晰的分层架构**
   - api → service → agent/tool → db/storage
   - 前端：View → Store → API
   - 每层职责单一，不跨层调用

9. **完整的类型系统**
   - Python: Pydantic Schema + SQLAlchemy Model + TypedDict
   - TypeScript: interface/type + Pinia store 类型推导

10. **异步全链路**
    - FastAPI async/await
    - httpx.AsyncClient
    - SQLAlchemy AsyncSession
    - APScheduler AsyncIOScheduler

---

## 21. 面试可能追问及回答思路

### Q1: "为什么选择 ReAct Agent 而不是其他 Agent 模式？"

**回答思路**：
- 项目经历了从固定编排到 ReAct 的演进
- 固定编排适合 MVP 阶段，但对复杂任务扩展性差
- ReAct 让模型可以根据中间观察动态调整下一步
- 但不是完全自由 Agent，是 Controlled ReAct
- 所有 action 必须在 Tool Registry 中，经过权限和业务规则校验

### Q2: "Tool Calling 为什么不用 LangChain？"

**回答思路**：
- LangChain 封装太重，本项目用原生 OpenAI Function Calling Schema
- 自己实现 ToolRegistry 可以精确控制权限、业务规则
- 代码更轻量，依赖更少，更容易理解和调试
- 相比 LangChain Agent，自实现更可控

### Q3: "多因子重排的权重是怎么确定的？"

**回答思路**：
- 语义相似度权重最高（0.45），因为这是核心需求
- 关键词匹配（0.20）作为补充，解决 Embedding 对专有名词不敏感的问题
- 新鲜度（0.15）和用户偏好（0.10）作为辅助
- 交互项（0.10）让"最新+最相关"的论文获得额外加分
- 权重可以根据实际数据做 A/B 测试调优

### Q4: "如果 Embedding API 不可用怎么办？"

**回答思路**：
- 有完整的 TF-IDF 兜底方案
- 纯 Python 实现，不依赖 sklearn/numpy
- 支持中英文混合分词
- 使用余弦相似度计算语义得分
- 实际上对于 arXiv 学术论文，TF-IDF 已经有不俗的效果

### Q5: "为什么要把 Skill 包装成 Tool？"

**回答思路**：
- 避免模型自由拼接底层原子工具
- 如果让模型自己决定"先调 arxiv_search，再调 rerank，再调 card_summary"，可能：
  - 跳过关键步骤
  - 参数传递错误
  - 违反业务规则（如搜索阶段做全文解析）
- Skill-as-Tool 模式把"已验证的稳定流程"包装为一个原子操作
- 既保留了灵活性，又保证了安全性

### Q6: "MCP Server 的作用是什么？"

**回答思路**：
- MCP = Model Context Protocol，标准化的 AI-工具交互协议
- 本项目实现 MCP Server，把论文助手能力暴露给外部 AI 客户端
- 例如 Claude Desktop 可以通过 MCP 调用本项目的 arxiv.search_papers
- Tools / Resources / Prompts 三层能力
- 使用 JSON-RPC 2.0 over stdio，协议简洁
- 关键设计：MCP 层是薄适配层，复用已有 business logic

### Q7: "Trace 系统为什么要每个 step 立即写入数据库？"

**回答思路**：
- 不是等任务结束再批量写
- 即使任务中途崩溃，也能看到已完成的步骤
- 方便实时调试和监控
- SQLite + aiosqlite 写入性能足够

### Q8: "如何保证论文卡片摘要不编造信息？"

**回答思路**：
- Prompt 中硬约束："你只能根据给定的内容总结，不能编造论文正文不存在的信息"
- 搜索阶段只传 title + abstract，不传全文
- summary_source 标记为 "metadata_only" 或 "abstract_intro_llm"
- 限制 LLM temperature 为 0.3（倾向于更确定性输出）

### Q9: "为什么选择 SQLite 而不是 PostgreSQL？"

**回答思路**：
- MVP 阶段优先简单（SQLite 零配置，本地运行）
- SQLAlchemy 抽象层保证后期可以无缝切换
- 修改 DATABASE_URL 环境变量即可从 SQLite 切换为 PostgreSQL
- SQLite 对于个人使用场景（单用户论文管理）完全足够

### Q10: "项目还有哪些可以改进的地方？"

**回答思路**：
- 全库问答 RAG（基于 pgvector + 论文全文）
- 多论文自动综述
- Related Work 自动写作
- Zotero / Obsidian 同步
- WebSocket 实时推送解析进度
- Redis 缓存热门搜索结果
- 更多 Skill 落地（paper_deep_parse_skill, daily_arxiv_digest_skill）

---

## 附录：关键文件索引

### 核心架构文件
| 文件 | 作用 |
|------|------|
| `ARCH.md` | 架构设计文档 |
| `PRD.md` | 产品需求文档 |
| `NEXT_UPGRADE_REACT_AGENT.md` | ReAct Agent 升级方案 |
| `backend/app/agent/orchestrator.py` | 主编排器 |
| `backend/app/agent/react_agent.py` | ReAct Agent 实现 |
| `backend/app/agent/intent_classifier.py` | 意图识别 |
| `backend/app/agent/tool_registry.py` | 工具注册中心 |
| `backend/app/agent/tool_schemas.py` | OpenAI Function Schema |
| `backend/app/agent/skill_registry.py` | 技能注册中心 |
| `backend/app/agent/bootstrap.py` | 工具注册引导 |

### 工具实现文件
| 文件 | 作用 |
|------|------|
| `backend/app/tools/arxiv_tool.py` | arXiv API 客户端 |
| `backend/app/tools/rerank_tool.py` | 多因子重排 |
| `backend/app/tools/embedding_tool.py` | 语义向量 + TF-IDF |
| `backend/app/tools/report_tool.py` | 论文摘要 + 精读报告 |
| `backend/app/tools/pdf_tool.py` | PDF 下载 + 解析 |
| `backend/app/tools/llm_client.py` | LLM 客户端（多厂商） |
| `backend/app/tools/trace_tool.py` | 全链路追踪 |
| `backend/app/tools/library_tool.py` | 本地库 CRUD |
| `backend/app/tools/notify_tool.py` | 邮件 + 飞书推送 |

### MCP Server 文件
| 文件 | 作用 |
|------|------|
| `backend/mcp_server/server.py` | MCP 服务入口 |
| `backend/mcp_server/tools/arxiv_tools.py` | MCP arXiv 工具 |
| `backend/mcp_server/tools/library_tools.py` | MCP 本地库工具 |
| `backend/mcp_server/tools/trace_tools.py` | MCP Trace 工具 |
| `backend/mcp_server/resources/library_resources.py` | MCP 论文资源 |
| `backend/mcp_server/prompts/paper_prompts.py` | MCP Prompt 模板 |

### 业务逻辑文件
| 文件 | 作用 |
|------|------|
| `backend/app/services/chat_service.py` | 聊天服务 |
| `backend/app/services/paper_service.py` | 论文收藏/解析服务 |
| `backend/app/jobs/daily_subscription_job.py` | 每日订阅执行 |
| `backend/app/jobs/scheduler.py` | 定时任务调度 |
| `backend/app/storage/file_manager.py` | 文件管理 |

### 前端核心文件
| 文件 | 作用 |
|------|------|
| `frontend/src/stores/chatStore.ts` | 聊天状态管理 |
| `frontend/src/components/chat/ChatWindow.vue` | 聊天窗口 |
| `frontend/src/components/chat/PaperCard.vue` | 论文卡片 |
| `frontend/src/components/trace/TraceTimeline.vue` | Trace 时间线 |
| `frontend/src/api/http.ts` | Axios 封装 |
| `frontend/src/router/index.ts` | 路由配置 |

---

*本文档基于项目实际代码生成，涵盖 arXiv 论文助手 Agent 的完整架构设计、核心流程、功能实现和面试要点。建议配合项目代码一起阅读，加深理解。*
